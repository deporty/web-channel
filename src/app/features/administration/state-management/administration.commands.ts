import { createAction } from '@ngrx/store';

export const GetRolesCommand = createAction('GetRolesCommand');
export const GetPermissionsCommand = createAction('GetPermissionsCommand');
export const GetResourcesCommand = createAction('GetResourcesCommand');
